# ============================================================
# requirements.txt — 宠物店智慧经营中枢
# 版本号是开发机上实测能跑通的版本，用 >= 而不是 == ，
# 免得卡死在小版本上装不起来。
#     pip install -r requirements.txt
# ============================================================

# ---------- Web 框架 ----------
fastapi>=0.139.0
uvicorn[standard]>=0.49.0          # [standard] 带上 watchfiles，reload=True 才生效
pydantic>=2.10.0

# ---------- 数据库 ----------
PyMySQL>=1.2.0                     # 直连 MySQL，不走 ORM（见 backend/database.py 的取舍说明）

# ---------- 配置 ----------
python-dotenv>=1.0.0

# ---------- 向量检索 ----------
numpy>=2.4.6                       # 必装：本地文件降级模式用它做余弦暴力检索
qdrant-client>=1.18.0              # 可选：装不上会自动降级到本地文件，不影响启动

# ---------- 工作流 ----------
langgraph>=1.2.0                   # 必装：routes.py 模块级 import，缺了服务起不来

# ---------- LLM ----------
openai>=2.35.1                     # 可选：SDK 缺了或 key 无效都会自动降级到规则引擎
